#!/usr/bin/env node
// TPMAP_PHASE34_BUILDING_ROOF_RECONSTRUCTION_INSTALLER
import path from "node:path";
import { fileURLToPath } from "node:url";
import { readFile, writeFile } from "node:fs/promises";
const here=path.dirname(fileURLToPath(import.meta.url)); const args=process.argv.slice(2); const i=args.indexOf("--generator"); const generator=i>=0?path.resolve(args[i+1]):null; const validateOnly=args.includes("--validate-only"); const selfTest=args.includes("--self-test");
if(selfTest) selfTestTransform(); else if(!generator) throw new Error("--generator is required"); else await install(generator,validateOnly);
async function install(root,validate){const moduleFile=path.join(root,"src/lib/building-roof-reconstruction.mjs"),testFile=path.join(root,"test/building-roof-reconstruction.test.mjs"),verticalFile=path.join(root,"src/lib/vertical-evidence-engine.mjs"); if(!validate){await writeFile(moduleFile,await readFile(path.join(here,"building-roof-reconstruction.mjs"),"utf8"));await writeFile(testFile,await readFile(path.join(here,"building-roof-reconstruction.test.mjs"),"utf8"));await writeFile(verticalFile,transformVerticalEngine(await readFile(verticalFile,"utf8")));} const moduleSource=await readFile(moduleFile,"utf8"),testSource=await readFile(testFile,"utf8"),verticalSource=await readFile(verticalFile,"utf8"); for(const t of ["TPMAP_PHASE34_BUILDING_ROOF_RECONSTRUCTION_V1","reconstructBuildingRoofs","validateBuildingReconstructions"])if(!moduleSource.includes(t))throw new Error(`Phase 34 building roof module missing ${t}`); if(!testSource.includes("generic sampler alone never fabricates a roof"))throw new Error("Phase 34 building roof tests incomplete"); validateVerticalEngine(verticalSource); console.log(JSON.stringify({status:validate?"validated":"installed",marker:"TPMAP_PHASE34_BUILDING_ROOF_RECONSTRUCTION_V1"}));}
export function transformVerticalEngine(source){if(source.includes("TPMAP_PHASE34_BUILDING_ROOF_INTEGRATION"))return source; let out=replaceOnce(source,'import { buildTerrainSurfaceModel, validateTerrainSurfaceModel } from "./terrain-surface-model.mjs";','import { buildTerrainSurfaceModel, validateTerrainSurfaceModel } from "./terrain-surface-model.mjs";\nimport { reconstructBuildingRoofs, validateBuildingReconstructions } from "./building-roof-reconstruction.mjs";\nconst TPMAP_PHASE34_BUILDING_ROOF_INTEGRATION = true;',"building roof import"); out=replaceOnce(out,'  diagnostics.terrainSurfaceModel = terrainSurfaceModel;\n  const rideVerticalProfiles = solveRideVerticalProfiles(graph, options);','  diagnostics.terrainSurfaceModel = terrainSurfaceModel;\n  const buildingRoofReconstruction = reconstructBuildingRoofs(graph, options.verticalSources || null, options);\n  validateBuildingReconstructions(graph);\n  diagnostics.buildingRoofReconstruction = buildingRoofReconstruction;\n  const rideVerticalProfiles = solveRideVerticalProfiles(graph, options);',"building roof stage"); validateVerticalEngine(out); return out;}
function validateVerticalEngine(s){for(const t of ["TPMAP_PHASE34_BUILDING_ROOF_INTEGRATION","reconstructBuildingRoofs(graph, options.verticalSources || null, options)","validateBuildingReconstructions(graph)","diagnostics.buildingRoofReconstruction = buildingRoofReconstruction"])if(!s.includes(t))throw new Error(`Phase 34 building roof integration missing ${t}`); const terrain=s.indexOf("buildTerrainSurfaceModel(graph, options.verticalSources || null, options)"),building=s.indexOf("reconstructBuildingRoofs(graph, options.verticalSources || null, options)"),ride=s.indexOf("solveRideVerticalProfiles(graph, options)"); if(!(terrain>=0&&building>terrain&&ride>building))throw new Error("Phase 34 building roof reconstruction must run after terrain surfaces and before ride/support reconstruction");}
function replaceOnce(source,before,after,label){if(source.includes(after))return source;const first=source.indexOf(before);if(first<0)throw new Error(`Phase 34 building roof anchor missing: ${label}`);if(source.indexOf(before,first+before.length)>=0)throw new Error(`Phase 34 building roof anchor ambiguous: ${label}`);return source.slice(0,first)+after+source.slice(first+before.length);}
function selfTestTransform(){const sample=['import { buildTerrainSurfaceModel, validateTerrainSurfaceModel } from "./terrain-surface-model.mjs";','export function solveParkVerticalEvidence(graph, options = {}) {','  const diagnostics = {};','  const terrainSurfaceModel = buildTerrainSurfaceModel(graph, options.verticalSources || null, options);','  validateTerrainSurfaceModel(graph);','  diagnostics.terrainSurfaceModel = terrainSurfaceModel;','  const rideVerticalProfiles = solveRideVerticalProfiles(graph, options);','}'].join("\n");const a=transformVerticalEngine(sample),b=transformVerticalEngine(a);if(a!==b)throw new Error("Phase 34 building roof transform not idempotent");validateVerticalEngine(a);console.log("Phase 34 building roof installer self-test passed");}
