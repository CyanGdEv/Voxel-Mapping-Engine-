# Alton Towers build-gate repair

The phone workflow now uses fixed Alton Towers benchmark bounds, installs production dependencies only, runs focused smoke tests, and captures setup/build diagnostics for failed runs.
